# 🎨 页面样式和功能修改指南

## 📋 快速查找表

| 要修改的内容 | 需要编辑的文件 | 位置 |
|------------|--------------|------|
| **全局主题色** | `app-config.js` | `theme.colors` |
| **整体布局样式** | `public/css/core.css` | 全部 |
| **侧边栏样式** | `public/css/core.css` | `.sidebar`, `.nav-item`, `.nav-header` |
| **菜单配置** | `app-config.js` | `menu` 数组 |
| **首页样式** | `public/css/core.css` + `modules/home/index.js` | 样式在CSS，内容在模块 |
| **博客页面** | `modules/blog/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **画廊页面** | `modules/gallery/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **动态页面** | `modules/moments/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **资源页面** | `modules/resources/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **游戏页面** | `modules/games/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **聊天页面** | `modules/chat/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **管理页面** | `modules/admin/index.js` + `public/css/modules.css` | 功能在模块，样式在CSS |
| **全局交互脚本** | `public/js/core.js` | 菜单切换等 |
| **应用交互脚本** | `public/js/app-interactions.js` | 点赞、评论等 |

---

## 🎨 样式修改

### 1. 修改全局主题色

**文件**: `app-config.js`

```javascript
theme: {
    colors: {
        primary: "#6366f1",      // 主色调（按钮、链接、强调色）
        secondary: "#ec4899",    // 次要色（辅助元素）
        accent: "#06b6d4",       // 强调色（特殊元素）
        background: "#0f172a",   // 背景色
        text: "#f8fafc",         // 主文字色
        textMuted: "#94a3b8"     // 次要文字色
    }
}
```

### 2. 修改整体布局样式

**文件**: `public/css/core.css`

- **侧边栏宽度**: 修改 `--sidebar-width` 变量（第 6 行）
- **侧边栏背景**: 修改 `.sidebar` 样式（第 48-58 行）
- **导航菜单样式**: 修改 `.nav-item` 样式（第 105-130 行）
- **分组标题样式**: 修改 `.nav-header` 样式（第 91-102 行）
- **卡片样式**: 修改 `.glass-card` 样式（第 170-190 行）
- **主内容区**: 修改 `.main-content` 和 `.content-wrapper`（第 140-155 行）

### 3. 修改特定页面样式

**文件**: `public/css/modules.css`

这个文件包含所有模块的特定样式：
- `.gallery-container` - 画廊布局
- `.moment-card` - 动态卡片
- `.resource-grid` - 资源网格
- `.game-grid` - 游戏网格
- `.chat-window` - 聊天窗口

---

## ⚙️ 功能修改

### 1. 修改首页

**文件**: `modules/home/index.js`

```javascript
// 修改首页卡片内容
const cards = config.menu.filter(...).map(m => 
    `<a href="${m.path}" class="glass-card">
        <!-- 修改这里的HTML结构 -->
    </a>`
).join('');

// 修改首页主标题和描述
content: `<div>
    <h1>${config.site.title}</h1>
    <p>${config.site.subtitle}</p>
    <!-- 添加更多内容 -->
</div>`
```

### 2. 修改博客页面

**文件**: `modules/blog/index.js`

```javascript
module.exports = {
    meta: { id: 'blog', name: '博客' },
    routes: [{
        path: '/',
        method: 'get',
        handler: (req, res) => {
            // 修改这里的博客内容
            const content = `<div class="glass-card">
                <h2>最新文章</h2>
                <!-- 添加文章列表 -->
            </div>`;
            res.send(render({ title: '博客', currentModule: 'blog', content }));
        }
    }]
};
```

### 3. 修改画廊页面

**文件**: `modules/gallery/index.js`

```javascript
// 修改图片数据
const ITEMS = [
    {src: '图片URL', title: '作品名称'},
    // 添加更多图片
];

// 修改画廊布局
const content = `<div class="gallery-container">
    ${ITEMS.map(i => `<div class="gallery-item">...</div>`).join('')}
</div>`;
```

### 4. 修改动态页面

**文件**: `modules/moments/index.js`

```javascript
// 修改动态内容结构
const content = `<div class="timeline-feed">
    <div class="moment-card">
        <!-- 修改动态卡片内容 -->
    </div>
</div>`;
```

### 5. 修改资源页面

**文件**: `modules/resources/index.js`

```javascript
// 修改资源分类映射
const MAP = {
    'unreal': {t:'虚幻素材', i:'fa-brands fa-unity'},
    'software': {t:'软件库', i:'fa-solid fa-plug'},
    // 添加更多分类
};

// 修改资源列表生成逻辑
const items = [1,2,3,4].map(i => ({
    title: `资源标题 ${i}`,
    cover: '封面图片URL',
    tag: '标签'
}));
```

### 6. 修改游戏页面

**文件**: `modules/games/index.js`

```javascript
// 修改游戏列表
const GAMES = [
    {n:'游戏名称', i:'fa-solid fa-icon'},
    // 添加更多游戏
];

// 修改游戏卡片布局
const content = `<div class="game-grid">
    ${GAMES.map(g => `<div class="game-card">...</div>`).join('')}
</div>`;
```

### 7. 修改聊天页面

**文件**: `modules/chat/index.js`

```javascript
// 修改聊天窗口结构
const content = `<div class="chat-window">
    <div class="chat-msgs">
        <!-- 修改消息显示区域 -->
    </div>
    <div class="chat-input">
        <!-- 修改输入框 -->
    </div>
</div>`;
```

### 8. 修改管理页面

**文件**: `modules/admin/index.js`

```javascript
// 修改管理功能
module.exports = {
    meta: { id: 'admin', name: '后台管理' },
    routes: [{
        path: '/',
        method: 'get',
        handler: (req, res) => {
            // 修改管理界面内容
            const content = `<div>管理功能</div>`;
            res.send(render({ title: '后台管理', currentModule: 'admin', content }));
        }
    }]
};
```

---

## 🔧 交互功能修改

### 1. 修改菜单交互

**文件**: `public/js/core.js`

```javascript
// 修改移动端菜单切换逻辑
document.addEventListener('DOMContentLoaded', () => {
    const toggle = document.querySelector('.menu-toggle');
    const sidebar = document.querySelector('.sidebar');
    // 修改这里的交互逻辑
});
```

### 2. 修改应用交互（点赞、评论等）

**文件**: `public/js/app-interactions.js`

```javascript
// 修改点赞功能
function toggleLike(btn) {
    // 修改点赞逻辑
}

// 添加新的交互函数
function newInteraction() {
    // 新功能
}
```

---

## ➕ 添加新页面/模块

### 步骤 1: 创建模块文件

在 `modules/` 目录下创建新文件夹，例如 `modules/my-page/`

创建 `modules/my-page/index.js`:

```javascript
const { render } = require('../../core/layout-engine');

module.exports = {
    meta: { id: 'my-page', name: '我的页面' },
    routes: [{
        path: '/',
        method: 'get',
        handler: (req, res) => {
            const content = `<div class="glass-card">
                <h1>我的页面</h1>
                <p>这是新页面的内容</p>
            </div>`;
            res.send(render({ 
                title: '我的页面', 
                currentModule: 'my-page', 
                content 
            }));
        }
    }]
};
```

### 步骤 2: 添加菜单项

编辑 `app-config.js`，在 `menu` 数组中添加：

```javascript
menu: [
    // ... 其他菜单项
    { id: 'my-page', label: '我的页面', path: '/my-page', icon: 'fa-solid fa-star' }
]
```

### 步骤 3: 添加样式（可选）

在 `public/css/modules.css` 中添加：

```css
.my-page-container {
    /* 你的样式 */
}
```

---

## 📝 常见修改场景

### 场景 1: 修改侧边栏颜色
- **文件**: `public/css/core.css`
- **位置**: `.sidebar` 样式（第 48-58 行）

### 场景 2: 修改卡片圆角
- **文件**: `public/css/core.css`
- **位置**: `.glass-card` 样式，修改 `border-radius`（第 170-190 行）

### 场景 3: 修改字体大小
- **文件**: `public/css/core.css`
- **位置**: `body` 样式或具体元素的 `font-size`

### 场景 4: 修改菜单项图标
- **文件**: `app-config.js`
- **位置**: `menu` 数组中对应项的 `icon` 属性

### 场景 5: 修改首页卡片布局
- **文件**: `modules/home/index.js`
- **位置**: `cards` 变量的 HTML 结构

### 场景 6: 添加新的资源分类
- **文件**: `modules/resources/index.js`
- **位置**: `MAP` 对象，添加新的分类映射

---

## ⚠️ 注意事项

1. **修改后需要重启服务器**才能看到效果
2. **清除浏览器缓存**（Ctrl+F5）确保样式更新
3. **备份重要文件**后再进行大改动
4. **CSS 变量**在 `app-config.js` 中定义，修改后全局生效
5. **模块文件**修改后立即生效（无需重启，但建议重启）

---

## 🎯 快速定位

- **找不到样式？** → 检查 `public/css/core.css` 或 `public/css/modules.css`
- **找不到功能代码？** → 检查 `modules/模块名/index.js`
- **找不到菜单配置？** → 检查 `app-config.js` 的 `menu` 数组
- **找不到交互脚本？** → 检查 `public/js/` 目录下的文件

---

**最后更新**: 2025-12-03

